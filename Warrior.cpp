#include "Warrior.h"

Warrior::Warrior(std::string name, float x, float y, int category, SDL_Window* window, SDL_Renderer* renderer) : Character(name, x, y, category, window, renderer, "warrior")
{

}
